
import {HomeModule} from './home/home.module';

export const DEFAULT_MODULES = [
  HomeModule
];

export const FULLSCREEN_MODULES = [];

export const MOBILE_MODULES = [];

export const PASSPORT_MODULES = [];
